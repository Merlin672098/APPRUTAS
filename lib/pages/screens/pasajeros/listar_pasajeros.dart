import 'package:flutter/material.dart';

class PasajerosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Bienvenido PASAJERO!',
        style: TextStyle(fontSize: 24),),
      ),
    );
  }
}
